spring:
  config:
    activate:
      on-profile: "redis"
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      seat-hold-ttl: ${REDIS_SEAT_HOLD:300}